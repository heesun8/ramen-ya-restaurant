import React from 'react';
import {images} from "../../constants"

const SubHeading = ({title}) => (
  <div style={{marginBottom: "1rem"}}>
    <p className="p__main">{title}</p>
    <div />
  </div>
);

export default SubHeading;
